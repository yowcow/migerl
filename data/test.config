[
    {"mysql", [
        {dialect, mysql},
        {config, [
            {host, "127.0.0.1"},
            {port, 3306},
            {user, "root"},
            {password, ""},
            {database, "migerl_test"},
            {log_warnings, false},
            {queries, [
                "SET NAMES utf8"
            ]}
        ]}
    ]},
    {"pg", [
        {dialect, postgres},
        {config, #{
            host => "127.0.0.1",
            username => "postgres",
            %password => "",
            database => "migerl_test",
            port => 5432
        }}
    ]}
].
